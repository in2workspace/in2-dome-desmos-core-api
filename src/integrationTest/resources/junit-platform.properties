# Registers ScorpioResetExtension (via META-INF/services) for every class in this source set, so
# broker isolation does not depend on each test author adding @ExtendWith. See ScorpioResetExtension.
junit.jupiter.extensions.autodetection.enabled=true
